<Window x:Class="MacroKids.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:editor="clr-namespace:MacroKids.NodeEditor.Controls;assembly=MacroKids.NodeEditor"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="720" Width="1200"
        Background="#1E1E2E" Foreground="#FFFFFF">

  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MacroKids.NodeEditor;component/Themes/DarkTheme.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MacroKids.NodeEditor;component/Themes/ControlStyles.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <!-- TitleBar / Controls Header -->
      <RowDefinition Height="60"/>
      <!-- Core Space -->
      <RowDefinition Height="*"/>
      <!-- StatusBar -->
      <RowDefinition Height="25"/>
    </Grid.RowDefinitions>

    <!-- Header Panel -->
    <Border Grid.Row="0" Background="#252538" BorderBrush="#2D2D44" BorderThickness="0,0,0,1" Padding="15,0">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Logo Placeholder / Label -->
        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
          <Image Source="pack://siteoforigin:,,,/Assets/MacroKids-MiniLogo.png" Width="32" Height="32" Margin="0,0,10,0" RenderOptions.BitmapScalingMode="HighQuality"/>
          <TextBlock Text="MacroKids" FontWeight="ExtraBold" FontSize="20" Foreground="#FFFFFF" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Quick Controls -->
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Content="Desfazer (Undo)" Command="{Binding CanvasViewModel.UndoCommand}" Margin="5,0" Padding="8,4" Background="#2D2D44" Foreground="White" BorderThickness="0"/>
          <Button Content="Refazer (Redo)" Command="{Binding CanvasViewModel.RedoCommand}" Margin="5,0" Padding="8,4" Background="#2D2D44" Foreground="White" BorderThickness="0"/>
          <Border Width="1" Background="#2D2D44" Margin="10,0"/>
          <Button Content="Executar (Run)" Background="#22C55E" Foreground="White" FontWeight="Bold" Padding="15,6" BorderThickness="0"/>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Central Editor Layout -->
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <!-- Sidebar Palette Catalog -->
        <ColumnDefinition Width="260"/>
        <ColumnDefinition Width="5"/>
        <!-- Main Canvas -->
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="5"/>
        <!-- Properties Panel -->
        <ColumnDefinition Width="300"/>
      </Grid.ColumnDefinitions>

      <!-- Sidebar -->
      <Border Grid.Column="0" Background="#252538" BorderBrush="#2D2D44" BorderThickness="0,0,1,0" Padding="10">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          
          <TextBlock Grid.Row="0" Text="MÓDULOS DISPONÍVEIS" FontSize="11" FontWeight="Bold" Foreground="#8F9CAE" Margin="0,0,0,10"/>
          
          <ListBox Grid.Row="1" ItemsSource="{Binding AvailableNodes}" Background="Transparent" BorderThickness="0">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Border BorderThickness="1" BorderBrush="#2D2D44" CornerRadius="6" Padding="10" Margin="0,4" Background="#2D2D44">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="{Binding Name}" FontWeight="Bold" Foreground="White" FontSize="13"/>
                    <TextBlock Grid.Row="1" Text="{Binding Description}" FontSize="11" Foreground="#8F9CAE" TextWrapping="Wrap" Margin="0,4,0,8"/>
                    <Button Grid.Row="2" Content="Adicionar" Padding="6,3" FontSize="11" 
                            Command="{Binding DataContext.AddNodeToCanvasCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                            CommandParameter="{Binding}" Background="#3B82F6" Foreground="White" BorderThickness="0" HorizontalAlignment="Left"/>
                  </Grid>
                </Border>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </Grid>
      </Border>

      <!-- Splitter 1 -->
      <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#1E1E2E"/>

      <!-- Canvas Area -->
      <editor:NodeEditorControl Grid.Column="2" DataContext="{Binding CanvasViewModel}"/>

      <!-- Splitter 2 -->
      <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="#1E1E2E"/>

      <!-- Properties Sidebar -->
      <Border Grid.Column="4" Background="#252538" BorderBrush="#2D2D44" BorderThickness="1,0,0,0" Padding="15">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <TextBlock Grid.Row="0" Text="PROPRIEDADES DO BLOCO" FontSize="11" FontWeight="Bold" Foreground="#8F9CAE" Margin="0,0,0,15"/>

          <StackPanel Grid.Row="1" DataContext="{Binding CanvasViewModel.SelectedNode}">
            <StackPanel.Style>
              <Style TargetType="StackPanel">
                <Style.Triggers>
                  <Trigger Property="DataContext" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                  </Trigger>
                </Style.Triggers>
              </Style>
            </StackPanel.Style>

            <TextBlock Text="Bloco Selecionado" FontSize="12" Foreground="#8F9CAE"/>
            <TextBlock Text="{Binding Metadata.Name}" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,2,0,15"/>

            <TextBlock Text="Descrição" FontSize="12" Foreground="#8F9CAE"/>
            <TextBlock Text="{Binding Metadata.Description}" FontSize="12" Foreground="White" TextWrapping="Wrap" Margin="0,2,0,20"/>

            <CheckBox IsChecked="{Binding IsDisabled}" Content="Desabilitar bloco" Foreground="White" Margin="0,0,0,15"/>
            
            <TextBlock Text="Comentário" FontSize="12" Foreground="#8F9CAE" Margin="0,5,0,2"/>
            <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Background="#1E1E2E" Foreground="White" BorderBrush="#2D2D44" Padding="6"/>
          </StackPanel>
        </Grid>
      </Border>
    </Grid>

    <!-- Footer Bar -->
    <Border Grid.Row="2" Background="#1E1E2E" BorderBrush="#2D2D44" BorderThickness="0,1,0,0" Padding="10,2">
      <TextBlock Text="{Binding StatusMessage}" Foreground="#8F9CAE" FontSize="11" VerticalAlignment="Center"/>
    </Border>
  </Grid>
</Window>

