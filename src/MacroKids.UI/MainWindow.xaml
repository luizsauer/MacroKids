<Window x:Class="MacroKids.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:editor="clr-namespace:MacroKids.NodeEditor.Controls;assembly=MacroKids.NodeEditor"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" Height="720" Width="1200"
        Background="#1E1E2E" Foreground="#FFFFFF">

  <Window.Resources>
    <ResourceDictionary>
      <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MacroKids.NodeEditor;component/Themes/DarkTheme.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MacroKids.NodeEditor;component/Themes/ControlStyles.xaml" />
      </ResourceDictionary.MergedDictionaries>
    </ResourceDictionary>
  </Window.Resources>

  <Grid>
    <Grid.RowDefinitions>
      <!-- TitleBar / Controls Header -->
      <RowDefinition Height="60"/>
      <!-- Core Space -->
      <RowDefinition Height="*"/>
      <!-- StatusBar -->
      <RowDefinition Height="25"/>
    </Grid.RowDefinitions>

    <!-- Header Panel -->
    <Border Grid.Row="0" Background="#181824" BorderBrush="#2D2D44" BorderThickness="0,0,0,1" Padding="15,0">
      <Grid>
        <Grid.ColumnDefinitions>
          <ColumnDefinition Width="Auto"/>
          <ColumnDefinition Width="*"/>
          <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <!-- Logo Label -->
        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
          <Border CornerRadius="6" Background="#3B82F6" Width="28" Height="28" Margin="0,0,10,0" VerticalAlignment="Center">
            <TextBlock Text="M" FontWeight="ExtraBold" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
          </Border>
          <TextBlock Text="MacroKids" FontWeight="ExtraBold" FontSize="20" Foreground="#FFFFFF" VerticalAlignment="Center" FontFamily="Nunito"/>
        </StackPanel>

        <!-- Quick Controls -->
        <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
          <Button Content="Desfazer" Command="{Binding CanvasViewModel.UndoCommand}" Margin="4,0" Width="90" Height="32" Background="#252538" Foreground="White" BorderBrush="#3B82F6" BorderThickness="1" FontSize="11" FontWeight="Bold"/>
          <Button Content="Refazer" Command="{Binding CanvasViewModel.RedoCommand}" Margin="4,0" Width="90" Height="32" Background="#252538" Foreground="White" BorderBrush="#3B82F6" BorderThickness="1" FontSize="11" FontWeight="Bold"/>
          <Border Width="1" Background="#2D2D44" Margin="10,5"/>
          <Button Content="▶ Executar" Background="#22C55E" Foreground="White" FontWeight="Bold" Width="100" Height="32" BorderThickness="0" FontSize="12"/>
        </StackPanel>
      </Grid>
    </Border>

    <!-- Central Editor Layout -->
    <Grid Grid.Row="1">
      <Grid.ColumnDefinitions>
        <!-- Sidebar Palette Catalog -->
        <ColumnDefinition Width="260"/>
        <ColumnDefinition Width="5"/>
        <!-- Main Canvas -->
        <ColumnDefinition Width="*"/>
        <ColumnDefinition Width="5"/>
        <!-- Properties Panel -->
        <ColumnDefinition Width="300"/>
      </Grid.ColumnDefinitions>

      <!-- Sidebar -->
      <Border Grid.Column="0" Background="#181824" BorderBrush="#2D2D44" BorderThickness="0,0,1,0" Padding="12">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          
          <TextBlock Grid.Row="0" Text="BLOCO NATIVOS" FontSize="11" FontWeight="Bold" Foreground="#8F9CAE" Margin="0,0,0,15" FontFamily="Nunito"/>
          
          <ListBox Grid.Row="1" ItemsSource="{Binding AvailableNodes}" Background="Transparent" BorderThickness="0">
            <ListBox.ItemTemplate>
              <DataTemplate>
                <Border BorderThickness="2" BorderBrush="#2D2D44" CornerRadius="8" Padding="12" Margin="0,6" Background="#252538">
                  <Grid>
                    <Grid.RowDefinitions>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="Auto"/>
                      <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Title with visual category badge style -->
                    <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,6">
                      <Ellipse Width="10" Height="10" Margin="0,0,8,0">
                        <Ellipse.Style>
                          <Style TargetType="Ellipse">
                            <Setter Property="Fill" Value="#5A6E85"/>
                            <Style.Triggers>
                              <DataTrigger Binding="{Binding Category}" Value="Mouse">
                                <Setter Property="Fill" Value="{StaticResource BrushMouse}" />
                              </DataTrigger>
                              <DataTrigger Binding="{Binding Category}" Value="Keyboard">
                                <Setter Property="Fill" Value="{StaticResource BrushKeyboard}" />
                              </DataTrigger>
                            </Style.Triggers>
                          </Style>
                        </Ellipse.Style>
                      </Ellipse>
                      <TextBlock Text="{Binding Name}" FontWeight="Bold" Foreground="White" FontSize="14" FontFamily="Nunito"/>
                    </StackPanel>
                    
                    <TextBlock Grid.Row="1" Text="{Binding Description}" FontSize="11" Foreground="#8F9CAE" TextWrapping="Wrap" FontFamily="Nunito" Margin="0,0,0,10"/>
                    <Button Grid.Row="2" Content="+ Adicionar" Padding="10,5" FontSize="11" FontWeight="Bold"
                            Command="{Binding DataContext.AddNodeToCanvasCommand, RelativeSource={RelativeSource AncestorType=Window}}" 
                            CommandParameter="{Binding}" Background="#3B82F6" Foreground="White" BorderThickness="0" HorizontalAlignment="Stretch">
                      <Button.Resources>
                        <Style TargetType="Border">
                          <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                      </Button.Resources>
                    </Button>
                  </Grid>
                </Border>
              </DataTemplate>
            </ListBox.ItemTemplate>
          </ListBox>
        </Grid>
      </Border>

      <!-- Splitter 1 -->
      <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Background="#1E1E2E"/>

      <!-- Canvas Area -->
      <editor:NodeEditorControl Grid.Column="2" DataContext="{Binding CanvasViewModel}"/>

      <!-- Splitter 2 -->
      <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch" Background="#1E1E2E"/>

      <!-- Properties Sidebar -->
      <Border Grid.Column="4" Background="#252538" BorderBrush="#2D2D44" BorderThickness="1,0,0,0" Padding="15">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>

          <TextBlock Grid.Row="0" Text="PROPRIEDADES DO BLOCO" FontSize="11" FontWeight="Bold" Foreground="#8F9CAE" Margin="0,0,0,15"/>

          <StackPanel Grid.Row="1" DataContext="{Binding CanvasViewModel.SelectedNode}">
            <StackPanel.Style>
              <Style TargetType="StackPanel">
                <Style.Triggers>
                  <Trigger Property="DataContext" Value="{x:Null}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                  </Trigger>
                </Style.Triggers>
              </Style>
            </StackPanel.Style>

            <TextBlock Text="Bloco Selecionado" FontSize="12" Foreground="#8F9CAE"/>
            <TextBlock Text="{Binding Metadata.Name}" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,2,0,15"/>

            <TextBlock Text="Descrição" FontSize="12" Foreground="#8F9CAE"/>
            <TextBlock Text="{Binding Metadata.Description}" FontSize="12" Foreground="White" TextWrapping="Wrap" Margin="0,2,0,20"/>

            <CheckBox IsChecked="{Binding IsDisabled}" Content="Desabilitar bloco" Foreground="White" Margin="0,0,0,15"/>
            
            <TextBlock Text="Comentário" FontSize="12" Foreground="#8F9CAE" Margin="0,5,0,2"/>
            <TextBox Text="{Binding Comment, UpdateSourceTrigger=PropertyChanged}" Background="#1E1E2E" Foreground="White" BorderBrush="#2D2D44" Padding="6"/>
          </StackPanel>
        </Grid>
      </Border>
    </Grid>

    <!-- Footer Bar -->
    <Border Grid.Row="2" Background="#1E1E2E" BorderBrush="#2D2D44" BorderThickness="0,1,0,0" Padding="10,2">
      <TextBlock Text="{Binding StatusMessage}" Foreground="#8F9CAE" FontSize="11" VerticalAlignment="Center"/>
    </Border>
  </Grid>
</Window>

