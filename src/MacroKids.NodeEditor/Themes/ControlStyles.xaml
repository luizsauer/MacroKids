<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:local="clr-namespace:MacroKids.NodeEditor.Controls">

  <!-- Pin View Template / Style -->
  <Style TargetType="{x:Type ContentControl}" x:Key="PinDotStyle">
    <Setter Property="Width" Value="12" />
    <Setter Property="Height" Value="12" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="{x:Type ContentControl}">
          <Grid>
            <Ellipse x:Name="OuterDot" Fill="Transparent" Stroke="#5A6E85" StrokeThickness="2" Width="12" Height="12"/>
            <Ellipse x:Name="InnerDot" Fill="#5A6E85" Width="6" Height="6" Visibility="Collapsed"/>
          </Grid>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="OuterDot" Property="Stroke" Value="#FFFFFF" />
              <Setter TargetName="OuterDot" Property="StrokeThickness" Value="3" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
